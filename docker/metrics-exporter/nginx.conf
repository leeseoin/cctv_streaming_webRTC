server {
    listen 80;
    charset utf-8;

    location / {
        root /usr/share/nginx/html;
        types {
            text/yaml yml;
            application/json json;
        }
        default_type text/yaml;
        charset_types text/yaml;
        add_header Access-Control-Allow-Origin *;
    }
}
